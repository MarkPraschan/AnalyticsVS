---
import QuickAnswerCallout from './QuickAnswerCallout.astro';
import type { ToolReview } from '../lib/reviews';

interface Props {
  review: ToolReview;
  pageUrl: string;
}

const { review, pageUrl } = Astro.props;
---

<QuickAnswerCallout content={review.quickAnswer} pageUrl={pageUrl} />
