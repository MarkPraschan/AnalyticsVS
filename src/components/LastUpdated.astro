---
import { formatDate } from '../lib/site';

interface Props {
  date: Date | string;
}

const { date } = Astro.props;
const isoDate = typeof date === 'string' ? date : date.toISOString();
---

<p class="last-updated" role="note">
  Updated <time datetime={isoDate}>{formatDate(date)}</time>
</p>
